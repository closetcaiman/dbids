[project]
name = "retail-generator"
version = "0.1.0"
description = "Synthetic retail dataset + SQLite builder for the lab9/lab10 generator image."
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2",
    "numpy>=1.26",
]

[tool.uv]
package = false
